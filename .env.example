# Media Explorer Configuration

# Server port
PORT=3000

# Root directory for media browsing (cannot navigate above this)
MEDIA_ROOT=/path/to/media

# Optional: Set to 'production' for production builds
NODE_ENV=development

# Docker network name
DOCKER_NETWORK=vision_network
