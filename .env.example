# Media Explorer Configuration

# Server port
PORT=3000

# Host path to mount as media directory (Docker only)
MEDIA_ROOT=/media

# Optional: Set to 'production' for production builds
NODE_ENV=development

# Root directory for media browsing (cannot navigate above this)
MEDIA_HOST_PATH=/path/to/media

# Maximum depth for recursive subfolder search (default: 5)
MAX_RECURSION_DEPTH=5

# Docker network name
DOCKER_NETWORK=vision_network
